# Supabase (ใส่เมื่อพร้อมใช้จริง — Phase 1 ใช้ demo auth ได้โดยไม่ต้องตั้งค่านี้)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# ชื่อ cookie session สำหรับ demo auth Phase 1
# DEMO_AUTH_ONLY=true

# Email (Resend) — ใส่เมื่อต้องการส่งอีเมลจริง (Phase 5)
# ถ้าเว้นว่าง ระบบจะบันทึก log เป็น skipped แทนการส่ง
RESEND_API_KEY=
EMAIL_FROM=SupplyStock <onboarding@resend.dev>
